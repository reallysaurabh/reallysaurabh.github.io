theme: jekyll-theme-minimal
logo: /assets/img/logo.jpg
google_analytics: UA-166757407-1

# Source-only files that GitHub Pages should not publish. The blog is built
# from blogs_list/ into blog/ (and the homepage into index.html) by
# `node blogs_list/build.js`; commit the generated output.
exclude:
  - blogs_list
  - aragorn-main.zip
  - 1758179000175.png
  - node_modules
